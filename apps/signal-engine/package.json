{
  "name": "signal-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "make dev",
    "build": "echo 'Python: no build step required'",
    "typecheck": "uv run --extra dev mypy src",
    "lint": "uv run --extra dev ruff check src"
  }
}
