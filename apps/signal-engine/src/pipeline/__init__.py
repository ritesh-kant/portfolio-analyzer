"""Pipeline package (stub).

The LangGraph-based agent pipeline (graph.py, agents/, state.py) was removed
during the demolition phase. The new pipeline will live under
apps/signal-engine/quant/ — see the rebuild plan for layout.
"""
