{
  "name": "analytics-service",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "serverless offline",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@portfolio-analyzer/analytics-core": "workspace:*",
    "@portfolio-analyzer/auth-utils": "workspace:*",
    "@portfolio-analyzer/shared-types": "workspace:*",
    "@portfolio-analyzer/tax-core": "workspace:*",
    "mongoose": "^8.9.2"
  },
  "devDependencies": {
    "serverless": "^4.5.0",
    "serverless-offline": "^14.4.0"
  }
}
